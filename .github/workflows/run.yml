name: Sample actions

on:
  push:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        run: echo Hello, world!
      - uses: actions/checkout@v2
      - run: |
          ls -al
          cd .github/workflows/
          ls -al